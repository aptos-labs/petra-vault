import { AccountAddress } from '@aptos-labs/ts-sdk';

export const Endpoints = {
  PETRA_PRICING: 'https://petra-pricing.petra-wallet.workers.dev'
};

export const LONG_FRAMEWORK_ADDRESS = AccountAddress.from('0x1').toStringLong();
